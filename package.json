{
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "vite": "^4.1.4"
  },
  "scripts": {
    "start": "vite",
    "host": "vite --host",
    "build": "vite build",
    "preview": "vite build && vite preview",
    "deploy": "vite build --base=/job-search-website-prototype/ && gh-pages -d dist"
  },
  "dependencies": {
    "firebase": "^9.17.2",
    "lit-html": "^2.6.1"
  }
}
